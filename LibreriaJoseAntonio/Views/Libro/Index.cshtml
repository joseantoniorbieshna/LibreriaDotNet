@model IEnumerable<LibreriaJose.Models.Data.Libro>

@{
    ViewBag.Title = "Index";
}
<style>

    .all_product {
        display: flex;
        flex-wrap:wrap;
        justify-content: center;
    }

    .container_product {
        border: 1px solid black;
        padding: 1.5rem 4rem 1.5rem 4rem;
        position:relative;
    }
    .product_img {
        max-width:15rem;
    }
    .info_book p {
        margin:0;
    }
    .title {
        font-weight:bold;
    }
    .autor {
        color: dimgray;
    }
    .precio {
        font-size: 1.5rem;
        color: red;
    }
    .container_product:hover .menu_info {
        visibility: visible;
        transition: opacity 0.2s linear;
        opacity:1;
    }
    .menu_info {
        background-color: white;
        border-width:1px 0px 0px 0px;
        border-style:solid;
        border-radius: 20px 20px 0px 00px;
        position: absolute;
        width: 100%;
        right: 0;
        bottom: 0;
        display:flex;
        flex-direction: column;
        align-items: center;
        visibility:hidden;
        opacity:0;
    }
    .mybutton {
        display:block;
        width:fit-content;
        margin:0.5rem;
    }
</style>


<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<div class="all_product">
    @foreach (var item in Model)
    {
        <div class="container_product">
            <img src="@item.Imagen" class="img-fluid product_img" />
            <div class="info_book ">
                <p class="title text-center">@Html.DisplayFor(modelItem => item.Titulo)</p>
                <p class="autor text-center">@Html.DisplayFor(modelItem => item.Autor_id.Nombre)</p>

            </div>
            <div class="precio text-center">
                @Html.DisplayFor(modelItem => item.Precio)€
            </div>
            <div class="menu_info">
                @Html.ActionLink("Edit", "Edit", new { id = item.ISBN }, new { @class = "mybutton btn btn-primary btn-lg" })
                @Html.ActionLink("Details", "Details", new { id = item.ISBN }, new {@class= "mybutton btn btn-primary btn-lg" })
                @Html.ActionLink("Delete", "Delete", new { id = item.ISBN }, new { @class = "mybutton btn btn-primary btn-lg" })
            </div>

        </div>

        @*     <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Autor_id.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Editorial_id.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Estado_id.Nombre)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Formato_id.Nombre)
                </td>
                <td>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Precio)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Cantidad)
                </td>

                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.ISBN }) |
                    @Html.ActionLink("Details", "Details", new { id = item.ISBN }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.ISBN })
                </td>
            </tr>*@
    }
</div>

</table>
